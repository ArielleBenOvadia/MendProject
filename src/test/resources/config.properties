# Google Cloud Storage CLI Testing Configuration

# Google Cloud project ID (required)
test.projectId=mend-project-arielle-123

# Test bucket name (use the bucket you created manually)
# You need to create this bucket manually in Google Cloud Console
test.bucket=gcloud-cli-test-bucket-1234

# Command timeout in seconds
command.timeout=60

# Browser configurations for URL validation
browser.headless=true
browser.timeout=30